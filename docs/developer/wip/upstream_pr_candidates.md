# Upstream Pull-Request Candidates

Every defect found in an external IP that deserves fixing at its source, in one place. The registry patches in `ollivander_config.yml` and the pre-build scripts are *our* remedies and keep working regardless; this list tracks the half of the work that belongs upstream, so it survives the session that discovered it. When a PR is filed or merged, update the status; when the fix lands in a revision we pin, drop the corresponding patch and the entry together.

A lesson recorded with the list, learned on 2026-08-03: **verify a defect at the repository's HEAD before calling it a PR candidate.** A defect upstream has already fixed calls for a pin bump, not a PR; and one upstream never had — because its requirement matrix differs from ours — is not theirs to fix. Both cases looked identical to the session that found them.

## Candidates toward pulp-platform (and neighbours)

| Repository | Defect | Our remedy today | Verified at HEAD | Status |
| :--- | :--- | :--- | :--- | :--- |
| `cva6` | The scalar-cryptography sources are missing from the Bender manifest, so the aes unit does not compile from a clean checkout | `scripts/patch_cva6_aes.py` compiles them, renaming to `cva6_aes_*` where opentitan also declares an `aes` | 2026-07-29 (found) | candidate |
| `cheshire` | `cheshire_pkg.sv` assigns `ret.slink` only under `if (cfg.SerialLink)` yet sets `ret.apb_mask[ret.slink]` unconditionally: with the link disabled the mask of an unrelated peripheral is raised and `reg_out_rsp[0]` is driven twice (vopt-7045) | registry patch guarding the assignment | 2026-07-29 (found) | candidate |
| `axi_obi` | `axi_to_obi.sv` slices `rsp_ruser[IdRuserWidth-1:ObiCfg.IdWidth]`, degenerate (reversed range) whenever `RUserWidth == 0` | registry patch guarding the two assigns | 2026-07-30 (found) | candidate |
| `obi` | `obi_to_apb.sv` uses the assertion macros without the `OBI_ASSERTS_OFF` guard its twin `apb_to_obi.sv` has — a cosmetic upstream inconsistency; the Verilator breakage that motivated our patch stopped reproducing once the ibex prim-assert signatures were aligned | none needed since 2026-08-07 (registry patch removed by the audit; the guard it added was provably inert) | 2026-08-03 found; 2026-08-07 obsoleted | dropped (inconsistency not worth a PR on its own) |
| `hier-icache` | The SVA property block in `share_icache.sv` was unparsable by Verilator 5.041 (`##[0:1]` cycle-delay range) | none needed since 2026-08-07: Verilator 5.050 parses it, and the registry patch was removed by the audit | 2026-08-03 found (5.041); 2026-08-07 obsoleted (5.050) | dropped (fixed by tool evolution, nothing to report upstream) |
| `neureka` | `neureka_ctrl.sv` uses `9'h0ffffffff`, a 36-bit literal in a 9-bit case item; simulators that do not truncate silently refuse it | registry patch replacing it with the equivalent `9'h1ff` | 2026-08-03, still present at HEAD; re-verified failing on Verilator 5.050, 2026-08-07 (registry audit) | candidate |
| `floo_noc` | `floo_nw_join` derives its internal ID-converter ports from the numeric `AxiCfg*` structs while its own ports carry the handed-in channel types; nothing enforces their agreement (module hygiene — see `future_evolution_tasks.md`, section 3.4) | none needed: everything Ollivander emits keeps the two consistent | 2026-07-30 (analysis) | candidate |
| `floo_noc` | With collectives enabled the `collect_op_e` enum is assigned from a raw 4-bit vector without a cast (~250 messages per run, the reason message 8386 is suppressed); no check that a flit carries a legal opcode | suppression of message 8386 (QuestaSim) and `-Wno-ENUMVALUE` (Verilator), volume only | 2026-07-28 (found) | candidate |
| `verilator` | `force` through an *unpacked* composite dies with an internal error (`V3Force.cpp: Unsupported: opaque force path selector ARRAYSEL`); a 30-line struct repro is banked in the session ledger and issue #7677 already tracks the unpacked-array case | packed-ification of the PeakRDL `field_storage_t` (catalogue patch for cheshire, generator post-process for our regblocks) | 2026-08-04 (found), #7677 open upstream | to file (comment on #7677) |
| `verilator` | Hierarchical verilation dies with an internal error on Verilator's OWN generated file (`__hierParameters.v:2: V3LinkDot.cpp:496: Module/etc never assigned a symbol entry?`, on the leading `typedef struct packed {`): serializing a hier_block's parameterization emits the boundary struct types as typedefs with nested anonymous packed structs, which it then cannot consume. **Diagnosis corrected 2026-08-06**: first attributed to parameter defaults reading members of package struct constants — a probe with literal defaults and parent-driven overrides reached the same error, so the defaults were only the first symptom and the struct types at the boundary are the cause | the generated `.vlt` excludes such modules from hierarchical verilation; the planned real fix is a flat generated shell whose ports are plain vectors | 2026-08-05 (found), 2026-08-06 (re-diagnosed, 5.050) | to file |
| `verilator` | `--binary` cannot be combined with `--hierarchical` (`--exe cannot be used together with --lib-create` from the child invocations) | the emitted flow uses the explicit `--cc --main --build` composition plus a manual executable link | 2026-08-04 (found, 5.050) | to file |
| `bender` | `script verilator` implicitly defines `TARGET_SYNTHESIS` with no opt-out, silently emptying every `ifndef TARGET_SYNTHESIS` simulation body in the unit (our reset generator above all) | `prep-sim-verilator` strips the define from the generated file list | 2026-08-05 (found, 0.31.0) | to file (opt-out request) |
| `hwpe-ctrl` | `hwpe_ctrl_slave` drives its packed `flags_o` struct output with mixed styles (four fields non-blocking in `always_ff`, the rest combinational): legal SV, but Verilator rejects it as unsupported (`BLKANDNBLK`) | registry patch moving the sequential fields to intermediate registers plus continuous assigns | 2026-08-04 (found) | candidate (Verilator-compatibility PR) |
| `peakrdl-regblock` | No option to emit the internal `field_storage` as a packed struct (deliberate design per the FAQ), which blocks `force` under Verilator | generator post-process packs it after export | 2026-08-05 (verified, 1.3.1) | to file (feature request `--packed-storage`) |
| `verilator` | The constant evaluator cannot read *unpacked*-struct constants (`SimulateVisitor: unknown node CONSPACKUORSTRUCT`, 5.050): a constant function copying `PulpClusterDefaultCfg` fails, and even with a literal parameter override, member selects like `Cfg.NumCores` do not fold as generate-for bounds or defparam values | the isle passes a full `'{...}` literal instead of a copy-then-override function; the in-IP failures were resolved on 2026-08-06 by packed-ifying `pulp_cluster_cfg_t`/`hwpe_subsystem_cfg_t` (see the pulp_cluster row below) — the Verilator limitation itself still stands and is what this entry reports | 2026-08-05 (found, 5.050, crux probes) | to file |
| `pulp_cluster` | `pulp_cluster_cfg_t` and `hwpe_subsystem_cfg_t` are unpacked structs though every member is packable, which trips the Verilator limitation above throughout the cluster; `cluster_bus_wrap.sv:148` also builds an `axi_pkg::xbar_cfg_t` literal without `default:`, so any added field (our fork's multicast pair) breaks elaboration | catalogue patches: packed-ification of both typedefs plus `default: '0` on the literal (2026-08-06) | 2026-08-05 (found, crux probes) | candidate (Verilator-compatibility PR) |
| `hci` | Three copy-paste typos in the ECC-handshake generate branches (`hci_core_mux_static.sv:121`, `hci_router.sv:282`, `hci_core_split.sv:273`): `r_evalid` is assigned from itself where the twin line derives `egnt` from `gnt` — a combinational self-loop, silently elaborated by QuestaSim (stuck ECC handshake, latent while nothing drives the cluster), refused by Verilator as circular logic | catalogue patches deriving each from the corresponding `r_valid` | 2026-08-06 (found, crux probe 6) | candidate (real functional defect; fix belongs in the CHIPS-IT fork too) |
| `spatz` | The cluster bootrom bakes its generation config's ABSOLUTE TCDM base into BOOTDATA and computes the entry-point register's address from it: wrong at any other instantiation base, and unfixable-by-value for two instances (the bootrom is one compile-time content shared by every instance). The sibling snitch_cluster bootrom solved this years ago with PC-relative arithmetic - but that pattern additionally requires the bootrom to live at a fixed offset from the peripherals, whereas spatz places it at absolute 'h1000, disjoint from the cluster block | registry patch rewrites the entry sequence to PC-relative (auipc/addi/lw/jr, hand-assembled in the ROM literal) and `spatz_cluster_isle.sv` relocates the bootrom to cluster_base + 'h3_0000 via BootAddr | 2026-08-07 (found, offload phase 2 bring-up) | candidate (adopt a PC-relative bootrom adjacent to the peripherals, like snitch_cluster's) |
| `hier-icache` | `HIERARCHY_ICACHE_32BIT` is an undocumented integrator obligation: with a 32-bit core fetch width, `pri_icache_controller.sv` slices the requested word out of its 128-bit line only under that symbol and otherwise serves word 0 for EVERY fetch — no error, wrong hardware (each line's first instruction executes four times). Nothing declares it: not the Bender manifest, not a README, not an elaboration-time check; carfield-family integrators define it globally in their own makefiles, so the trap only fires on a fresh integration | the pulp_cluster isle declares it through its `DEFINE:` compilation contract (2026-08-07, first cluster fetch ever in an Ollivander SoC) | 2026-08-07 (found) | candidate (declare it in the manifest's defines, or replace the silent fallback with `$fatal` when `FETCH_DATA_WIDTH != REFILL_DATA_WIDTH` and the symbol is absent) |

## Alignments toward the CHIPS-IT forks (not upstream defects)

| Repository | Change | Why it is ours |
| :--- | :--- | :--- |
| `FondazioneChipsIT/pulp_cluster` | Remove the seven dead geometry overrides (`AWC`, `DW_LIC`, `DW_SIC`, `AWH`, `DWH`, `OWH`, `AWM`) and the four `boffs`/`lrdy` interface ties in `cluster_interconnect_wrap.sv` / `idma_wrap.sv` | pulp-platform's master still carries these lines, consistently with the older hci *it* requires; the mismatch is created by our hci forcing, so the fix belongs next to it |
| `cva6` (vendor refresh, not a patch-PR) | The two vendored copies of `common_cells/assertions.svh` predate the modern macro signatures and carry the old `PRIM_ASSERT_SV` include guard. **Corrected 2026-08-04**: no include directory in our generated flists ever reaches them, so they proved inert — the actual single-unit macro clash came from ibex's vendored `prim_assert.sv` (same guard name, which misled the first diagnosis) and is repaired by the `ibex` catalogue patches. The vendored staleness remains true but currently harms nothing | vendor-directory staleness only; no remedy needed today |
